---
import {Icon} from "astro-icon/components";

interface Props {
  icon: string;
  href?: string;
}

const { icon, href: site } = Astro.props;
---

<a href={site} class="inline-flex items-center">
  <Icon name={icon} class="h-4 mr-1" />
  <span class="text-sm"><slot /></span>
</a>
